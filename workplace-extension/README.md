# workplace-extension

Manifest V3 browser extension for SendWiseWorkplace. See `../docs/EXTENSION_SPEC.md` for architecture and `../docs/TODO.md` Phase 3 for implementation checklist.

Reuses the SendWise Random Forest classifier from `../model_training/` (run `export_to_kotlin_json.py` — the JSON output is also loadable in JS).
